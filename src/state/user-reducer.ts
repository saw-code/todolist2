export const userReducer = (state: any, action: any) => {
  switch (action.type) {
    case "BLABLA1":

    case "YO":

    default:
      throw new Error("I Don't understand this action type")
  }
}
